/**
 * Length of `nanoid` to generate to construct user's unique id.
 */
export const NANOID_USERID_LENGTH = 25

/**
 * Number of rounds to generate password salt
 */
export const BCRYPT_GENSALT_ROUNDS = 10
